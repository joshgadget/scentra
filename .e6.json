[
 { "find": "import{ArrowLeft,ArrowRight,BarChart3,Check,Copy,", "replace": "import{AlertTriangle,ArrowLeft,ArrowRight,BarChart3,Check,Copy," },
 { "find": "</header>{error&&<div className='admin-error'>{error}<button onClick={load}>Retry</button></div>}",
   "replace": "</header>{dashboard.store&&dashboard.store.database!=='postgresql'&&<div className='admin-notice'><AlertTriangle/><div><strong>{dashboard.store.databaseConfigured?'Database unreachable - showing the local demo store':'Demo mode - no database connected'}</strong><span>{dashboard.store.databaseConfigured?`${dashboard.store.problem||'The database is temporarily unavailable.'} Everything you change here is saved to the local demo store until the connection is restored.`:'Everything you change here is saved to the local demo store on this server rather than PostgreSQL.'}</span>{dashboard.store.demoStore?.localChanges&&<small>{dashboard.store.demoStore.mutations} local change{dashboard.store.demoStore.mutations===1?'':'s'} not in PostgreSQL{dashboard.store.demoStore.savedAt?` - last saved ${new Date(dashboard.store.demoStore.savedAt).toLocaleString()}`:''}</small>}</div></div>}{error&&<div className='admin-error'>{error}<button onClick={load}>Retry</button></div>}" }
]