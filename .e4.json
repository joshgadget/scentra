[
 { "find": "seedProducts.unshift(product); return res.status(201).json(product) }",
   "replace": "seedProducts.unshift(product); persistDemoStore(); return res.status(201).json(product) }" },
 { "find": "price:Number(variant.price),stock:Number(variant.stock)})); return res.json(product) } const data={};",
   "replace": "price:Number(variant.price),stock:Number(variant.stock)})); persistDemoStore(); return res.json(product) } const data={};" },
 { "find": "if (index >= 0) seedProducts.splice(index, 1); return res.sendStatus(204) }",
   "replace": "if (index >= 0) { seedProducts.splice(index, 1); persistDemoStore() } return res.sendStatus(204) }" },
 { "find": "memoryCoupons.unshift(coupon); return res.status(201).json(coupon) }",
   "replace": "memoryCoupons.unshift(coupon); persistDemoStore(); return res.status(201).json(coupon) }" },
 { "find": "if(index>=0)memoryCoupons.splice(index,1); return res.sendStatus(204) }",
   "replace": "if(index>=0){memoryCoupons.splice(index,1);persistDemoStore()} return res.sendStatus(204) }" },
 { "find": "memorySettings[req.params.key]={...memorySettings[req.params.key],...req.body}; return res.json(memorySettings[req.params.key]) }",
   "replace": "memorySettings[req.params.key]={...memorySettings[req.params.key],...req.body}; persistDemoStore(); return res.json(memorySettings[req.params.key]) }" },
 { "find": "const order = memoryOrders.find((item) => item.id === req.params.id); if (order) order.status = req.body.status; return res.json(order || { ok:true }) }",
   "replace": "const order = memoryOrders.find((item) => item.id === req.params.id); if (order) { order.status = req.body.status; persistDemoStore() } return res.json(order || { ok:true }) }" },
 { "find": "      memoryOrders.unshift(order)\n    }",
   "replace": "      memoryOrders.unshift(order)\n      persistDemoStore()\n    }" },
 { "find": "if(coupon&&coupon.usedCount>0)coupon.usedCount-=1} }\n",
   "replace": "if(coupon&&coupon.usedCount>0)coupon.usedCount-=1} persistDemoStore() }\n" },
 { "find": "if (order && order.status === 'PENDING') { order.status = 'PAID'; order.paymentReference = reference; safeNotify(order, true) }",
   "replace": "if (order && order.status === 'PENDING') { order.status = 'PAID'; order.paymentReference = reference; persistDemoStore(); safeNotify(order, true) }" },
 { "find": "    else memorySubscribers.add(email)\n",
   "replace": "    else { memorySubscribers.add(email); persistDemoStore() }\n" },
 { "find": "    memoryCustomers.set(auth.email, customer)\n    return customer",
   "replace": "    memoryCustomers.set(auth.email, customer)\n    if (!existing) persistDemoStore()\n    return customer" },
 { "find": "    else memoryCustomers.set(email, { id:email, name, email, phone:null, createdAt:new Date().toISOString() })",
   "replace": "    else { memoryCustomers.set(email, { id:email, name, email, phone:null, createdAt:new Date().toISOString() }); persistDemoStore() }" }
]